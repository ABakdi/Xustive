# Written by the operator console (M12). Applied after the config file and the
# environment on every start; delete a key here to fall back to the config file.

[federation]
enabled = true
budget_ms = 900
fetch_budget_ms = 8000
max_hits = 25
eager_index = true

[collection]
enabled = true

[ml]

[interaction]
